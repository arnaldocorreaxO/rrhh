"""
Django settings for web project.

Generated by 'django-admin startproject' using Django 3.0.3.

For more information on this file, see
https://docs.djangoproject.com/en/3.0.3/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/3.0.3/ref/settings/
"""


from config import db

import os

# Build paths inside the project like this: os.path.join(BASE_DIR, ...)
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/3.0.3/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'sv3bn4w^rir+zz4@jh_cse38bi3sxh#3s-3ro@yybzrmk98=bp'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = ["*"]

# Application definition

ADMIN_THEME_APPS = [
    'admin_interface',
    # 'flat_responsive', # only if django version < 2.0
    # 'flat', # only if django version < 1.9
    'colorfield',
]

DJANGO_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
]

THIRD_PARTY_APPS = [
    'widget_tweaks',
    'import_export',
    
]

LOCAL_APPS = [    
    'core.security',
    'core.user',
    'core.login',
    'core.dashboard',
    'core.reports',
    'core.base',
    'core.asistencia',
]

INSTALLED_APPS = ADMIN_THEME_APPS + DJANGO_APPS + LOCAL_APPS + THIRD_PARTY_APPS

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    'crum.CurrentRequestUserMiddleware'
]

ROOT_URLCONF = 'config.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [os.path.join(BASE_DIR, 'templates')],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                'core.security.functions.system_information',
            ],
        },
    },
]

WSGI_APPLICATION = 'config.wsgi.application'

# Database
# https://docs.djangoproject.com/en/3.0.3/ref/settings/#databases

DATABASES = db.POSTGRESQL
# Password validation
# https://docs.djangoproject.com/en/3.0.3/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]

# Internationalization
# https://docs.djangoproject.com/en/3.0.3/topics/i18n/

LANGUAGE_CODE = 'es-PY'

TIME_ZONE = 'America/Asuncion'

USE_I18N = True

USE_L10N = True
# Si se activa representa 4 horas menos en TZ America/Asuncion
# USE_TZ = True


USE_DECIMAL_SEPARATOR = True
DECIMAL_SEPARATOR = ","
USE_THOUSAND_SEPARATOR = True
THOUSAND_SEPARATOR = "."

# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/3.0.3/howto/static-files/

STATIC_URL = '/static/'

STATICFILES_DIRS = [
    os.path.join(BASE_DIR, 'static'),
]

MEDIA_ROOT = os.path.join(BASE_DIR, 'media/')

STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles/')

MEDIA_URL = '/media/'

# Login

LOGIN_REDIRECT_URL = '/dashboard/'

LOGOUT_REDIRECT_URL = '/login/'

LOGIN_URL = '/login/'

AUTH_USER_MODEL = 'user.User'

# Email

EMAIL_USE_TLS = True
EMAIL_HOST = 'mail.in-planet.net'
EMAIL_PORT = 587
EMAIL_HOST_USER = 'arnaldo.correa@in-planet.net'
EMAIL_HOST_PASSWORD = 'inplaneT.2018'
EMAIL_BACKEND = 'core.libs.smtp.EmailBackend'

# Configurations

LOCALHOST = 'localhost'

SESSION_SERIALIZER = 'django.contrib.sessions.serializers.PickleSerializer'

SESSION_COOKIE_NAME = 'factora'

GROUPS = {
    'client': 2,
}


# CONFIGURACIONES DE REPORTES
REPORTS_DIR = os.path.join(os.path.abspath(BASE_DIR), 'core','reports','resources','')
# print(REPORTS_DIR)
PGSQL_JDBC_DIR = os.path.join(REPORTS_DIR,'postgresql-42.2.20.jar')

# from core.base.models import Reporte
# REPORTE_TITULO1 = Reporte.objects.filter(nombre_reporte = 'REPORTE_TITULO1').first()
# REPORTE_TITULO2 = Reporte.objects.filter(nombre_reporte = 'REPORTE_TITULO2').first()
# REPORTE_TITULO3 = Reporte.objects.filter(nombre_reporte = 'REPORTE_TITULO3').first()
REPORT_TITULO1 = 'INDUSTRIA NACIONAL DEL CEMENTO'
REPORT_TITULO2 = '"PUESTO DE PESAJE BASCULA VILLETA"'
REPORT_TITULO3 = 'INDUSTRIA NACIONAL DEL CEMENTO"'